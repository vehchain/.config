#!/bin/bash


#swaymsg -t subscribe '["window"]' | while read -r event; do
  #window_title=$(swaymsg -t get_tree | jq -r '.. | select(.type=="floating_con" or .type=="con") | select(.focused==true).name')
  #echo "$window_title"
  #echo "meow"
#done
#swaymsg -t subscribe '["window"]' | while read -r event; do
  #window_title=$(swaymsg -t get_tree | jq -r '.. | select(.type=="floating_con" or .type=="con") | select(.focused==true).name')
  #echo "$window_title"
  #echo "meow"
#done

